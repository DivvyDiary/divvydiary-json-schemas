const defs = require("./schemas/defs.json");
const dividend = require("./schemas/dividend.json");
const symbol = require("./schemas/symbol.json");
const user = require("./schemas/user.json");

module.exports = {
  defs,
  dividend,
  symbol,
  user,
};
